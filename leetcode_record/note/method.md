# Leetcode沉浮录

#### 001 Two sum

+ $\Theta(n^2)$ 解法

  + 5260

    暴力搜索，$\Theta(n^2)$。

  + 4492

    5260+将 `len(nums)` 放在循环外，作为常量

  + 768

    直接用 `in` 进行搜索，省下了 $\Theta(n)$ 的四则运算

  + 280

    减少重复的查询（将查询过的放入别的list里，进行比对）将复杂度减半。

+ $\Theta(n)$ 解法

  在280的基础上，将列表→字典，将搜索成本从 $\Theta(n)$ 降低到 $\Theta(1)​$。

  
#### 00

  